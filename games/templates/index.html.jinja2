<!DOCTYPE html>
<html lang="en-GB">

<head>
    <meta charset="UTF-8">
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/styles.css">
    <link rel="icon" href="./assets/favicon.png">
    <link rel="apple-touch-icon" sizes="180x180" href="./assets/favicon.png">
</head>

<body>

<main>
    <header>
        <h1>Video Game Catalog</h1>
    </header>
    <h2>Top Games of 2023</h2>
    <ul class="top-game-list">
        {% for top_game in top_games %}
        <li class="top-game"><span class="score">{{ top_game.topCriticScore }}</span> {{ top_game.name }}</li>
        {% endfor %}
    </ul>
    <h2>Recent Releases</h2>
    <div class="game-grid">
        {% for game in recent %}
        <div class="game">
            <h3>{{ game.name }}</h3>
            <p class="date">{{ game.firstReleaseDate }}</p>
            <a href="https://opencritic.com/game/{{ game.id }}/{{ game.name.lower().replace(' ', '-') }}">
            <img src="https://img.opencritic.com/{{ game.images.box.og }}">
            </a>
        </div>
        {% endfor %}
    </div>
    <h2>Upcoming Releases</h2>
    <div class="game-grid">
        {% for game in games %}
        <div class="game">
            <h3>{{ game.name }}</h3>
            <p class="date">{{ game.firstReleaseDate }}</p>
            <a href="https://opencritic.com/game/{{ game.id }}/{{ game.name.lower().replace(' ', '-') }}">
            <img src="https://img.opencritic.com/{{ game.images.box.og }}">
            </a>
        </div>
        {% endfor %}
    </div>
    <footer>
        <p>Created by David Manolitsas</p>
    </footer>
</main>
</body>

</html>
